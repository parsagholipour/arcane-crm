"use client";

import * as Dialog from "@radix-ui/react-dialog";
import { BookOpen, Box, Plus, Trash2, X } from "lucide-react";
import Link from "next/link";
import { useState, type ReactNode } from "react";
import type { BootstrapData, RecordData } from "@/lib/crm-types";
import { cn, formatDate, formatDateTime } from "@/lib/utils";

type Toast = { tone: "success" | "error" | "warning"; message: string } | null;
const input = "min-h-9 w-full rounded border border-[#c9c9c9] bg-white px-3 py-2 text-sm outline-none focus:border-[#0176d3] focus:ring-2 focus:ring-[#0176d3]/20 disabled:bg-[#f3f3f3]";
const secondary = "inline-flex min-h-8 items-center justify-center gap-1 rounded border border-[#c9c9c9] bg-white px-3 py-1 text-xs font-semibold text-brand-700 hover:bg-[#f3f3f3] disabled:opacity-50";
const primary = "inline-flex min-h-8 items-center justify-center gap-1 rounded border border-brand-700 bg-brand-600 px-3 py-1 text-xs font-semibold text-white hover:bg-brand-700 disabled:opacity-50";
function text(value: unknown) { return value === null || value === undefined ? "" : String(value); }
function id(record: RecordData) { return text(record.id); }
function money(value: unknown, currency = "USD") { return new Intl.NumberFormat("en-US", { style: "currency", currency }).format(Number(value ?? 0)); }
async function json(url: string, init?: RequestInit) { const response = await fetch(url, init); const payload = await response.json().catch(() => ({})); if (!response.ok) throw new Error(text(payload.error) || "The request failed."); return payload as Record<string, unknown>; }
function Modal({ title, onClose, footer, children }: { title: string; onClose: () => void; footer: ReactNode; children: ReactNode }) { return <Dialog.Root open onOpenChange={(open) => !open && onClose()}><Dialog.Portal><Dialog.Overlay className="fixed inset-0 z-[90] bg-black/40" /><Dialog.Content className="fixed left-1/2 top-1/2 z-[100] max-h-[90vh] w-[calc(100vw-2rem)] max-w-3xl -translate-x-1/2 -translate-y-1/2 overflow-hidden rounded-lg bg-white shadow-2xl"><div className="flex items-center justify-between border-b border-[#d8dde6] px-5 py-3"><Dialog.Title className="text-lg font-semibold">{title}</Dialog.Title><button className="rounded p-1 hover:bg-[#f3f3f3]" onClick={onClose}><X size={18} /></button></div><div className="max-h-[calc(90vh-120px)] overflow-auto p-5">{children}</div><div className="flex justify-end gap-2 border-t border-[#d8dde6] bg-[#f8f9fb] px-5 py-3">{footer}</div></Dialog.Content></Dialog.Portal></Dialog.Root>; }
function Field({ label, children }: { label: string; children: ReactNode }) { return <label className="block text-sm"><span className="mb-1 block text-xs font-semibold text-[#444]">{label}</span>{children}</label>; }
function Card({ title, children, action }: { title: string; children: ReactNode; action?: ReactNode }) { return <section className="rounded-lg border border-[#e4e7ec] bg-white shadow-card"><div className="flex items-center justify-between border-b border-[#d8dde6] px-4 py-3"><h2 className="font-semibold">{title}</h2>{action}</div><div className="p-4">{children}</div></section>; }
function Detail({ label, value }: { label: string; value: unknown }) { return <div><dt className="text-xs text-[#706e6b]">{label}</dt><dd>{text(value) || "-"}</dd></div>; }
function Active({ value }: { value: unknown }) { return <span className={cn("rounded-full px-2.5 py-1 text-xs font-semibold", value ? "bg-[#e4f6e6] text-[#194f25]" : "bg-[#f3f3f3] text-[#514f4d]")}>{value ? "Active" : "Inactive"}</span>; }

export function ProductDetailPage({ product, data, onDataChange, onDelete, onToast }: { product: RecordData; data: BootstrapData; onDataChange: (updater: (previous: BootstrapData) => BootstrapData) => void; onDelete: () => void; onToast: (toast: Toast) => void }) {
  const [edit, setEdit] = useState(false); const entries = data.priceBookEntries.filter((entry) => entry.productId === product.id); const inventory = data.inventoryItems.filter((item) => item.productId === product.id); const orderLines: RecordData[] = data.commerceOrders.flatMap((order) => (Array.isArray(order.lines) ? order.lines as RecordData[] : []).map((line) => ({ ...line, order } as RecordData))).filter((line) => line.productId === product.id);
  function saved(record: RecordData) { onDataChange((previous) => ({ ...previous, products: previous.products.map((item) => item.id === record.id ? { ...item, ...record } : item) })); setEdit(false); onToast({ tone: "success", message: "Product updated." }); }
  return <section className="space-y-3"><Header icon={<Box className="text-brand-600" />} eyebrow="Product" title={text(product.name)} badge={<Active value={product.active} />} actions={<><button className={secondary} onClick={() => setEdit(true)}>Edit</button><button className={secondary} onClick={onDelete}><Trash2 size={13} /> Delete</button></>} /><div className="grid gap-3 lg:grid-cols-2"><Card title="Product Details"><dl className="grid grid-cols-2 gap-3 text-sm"><Detail label="Product Code" value={product.productCode} /><Detail label="SKU" value={product.sku} /><Detail label="Family" value={product.family} /><Detail label="Category" value={product.category} /><Detail label="Created" value={formatDateTime(text(product.createdAt))} /><Detail label="Updated" value={formatDateTime(text(product.updatedAt))} /></dl></Card><Card title="Description"><div className="whitespace-pre-wrap text-sm">{text(product.description) || "No description."}</div></Card></div><Card title={`Price Book Entries (${entries.length})`}>{entries.length ? <div className="overflow-auto"><table className="min-w-full text-left text-sm"><thead className="bg-[#f3f3f3] text-xs"><tr><th className="px-3 py-2">Price Book</th><th className="px-3 py-2">List Price</th><th className="px-3 py-2">Currency</th><th className="px-3 py-2">State</th></tr></thead><tbody>{entries.map((entry) => <tr key={id(entry)} className="border-t border-[#eef1f6]"><td className="px-3 py-2"><Link className="font-semibold text-brand-700 hover:underline" href={`/lightning/r/Pricebook2/${text(entry.priceBookId)}/view`}>{text((entry.priceBook as RecordData | undefined)?.name) || text(data.priceBooks.find((book) => book.id === entry.priceBookId)?.name)}</Link></td><td className="px-3 py-2">{money(entry.listPrice, text(entry.currency))}</td><td className="px-3 py-2">{text(entry.currency)}</td><td className="px-3 py-2"><Active value={entry.active} /></td></tr>)}</tbody></table></div> : <Empty value="This Product is not in a Price Book yet. Add it from a Price Book detail page." />}</Card><div className="grid gap-3 lg:grid-cols-2"><Card title={`Inventory (${inventory.length} stores)`}>{inventory.map((item) => <div key={id(item)} className="flex justify-between border-b border-[#eef1f6] py-2 text-sm last:border-0"><span>{text((item.store as RecordData | undefined)?.name) || text(data.stores.find((store) => store.id === item.storeId)?.name)}</span><span>{text(item.quantityOnHand)} on hand · {text(item.quantityReserved)} reserved</span></div>)}{!inventory.length && <Empty value="Inventory is not tracked for this Product." />}</Card><Card title={`Order Usage (${orderLines.length})`}>{orderLines.slice(0, 8).map((line) => <div key={id(line)} className="flex justify-between border-b border-[#eef1f6] py-2 text-sm last:border-0"><span>{text((line.order as RecordData).orderNumber)}</span><span>{text(line.quantity)} × {money(line.unitPrice, text((line.order as RecordData).currency))}</span></div>)}{!orderLines.length && <Empty value="This Product has not been used on a commerce order." />}</Card></div>{edit && <ProductEditor product={product} onClose={() => setEdit(false)} onSaved={saved} />}</section>;
}

export function PriceBookDetailPage({ priceBook, data, onDataChange, onDelete, onToast }: { priceBook: RecordData; data: BootstrapData; onDataChange: (updater: (previous: BootstrapData) => BootstrapData) => void; onDelete: () => void; onToast: (toast: Toast) => void }) {
  const [edit, setEdit] = useState(false); const [entryModal, setEntryModal] = useState<RecordData | true | null>(null); const entries = data.priceBookEntries.filter((entry) => entry.priceBookId === priceBook.id); const stores = data.stores.filter((store) => store.priceBookId === priceBook.id);
  function saveBook(record: RecordData) { onDataChange((previous) => ({ ...previous, priceBooks: previous.priceBooks.map((item) => item.id === record.id ? { ...item, ...record } : item) })); setEdit(false); onToast({ tone: "success", message: "Price Book updated." }); }
  function saveEntry(entry: RecordData) { onDataChange((previous) => ({ ...previous, priceBookEntries: previous.priceBookEntries.some((item) => item.id === entry.id) ? previous.priceBookEntries.map((item) => item.id === entry.id ? entry : item) : [entry, ...previous.priceBookEntries] })); setEntryModal(null); onToast({ tone: "success", message: "Price Book entry saved." }); }
  async function deleteEntry(entry: RecordData) { if (!window.confirm(`Delete the ${text((entry.product as RecordData | undefined)?.name)} entry?`)) return; try { await json(`/api/price-books/${id(priceBook)}/entries/${id(entry)}`, { method: "DELETE" }); onDataChange((previous) => ({ ...previous, priceBookEntries: previous.priceBookEntries.filter((item) => item.id !== entry.id) })); onToast({ tone: "success", message: "Price Book entry deleted." }); } catch (error) { onToast({ tone: "error", message: error instanceof Error ? error.message : "Unable to delete entry." }); } }
  return <section className="space-y-3"><Header icon={<BookOpen className="text-brand-600" />} eyebrow="Price Book" title={text(priceBook.name)} badge={<Active value={priceBook.active} />} actions={<><button className={secondary} onClick={() => setEdit(true)}>Edit</button><button className={secondary} onClick={onDelete}><Trash2 size={13} /> Delete</button></>} /><div className="grid gap-3 lg:grid-cols-2"><Card title="Price Book Details"><dl className="grid grid-cols-2 gap-3 text-sm"><Detail label="Standard Price Book" value={priceBook.isStandard ? "Yes" : "No"} /><Detail label="Entries" value={entries.length} /><Detail label="Valid From" value={priceBook.validFrom ? formatDate(text(priceBook.validFrom)) : "-"} /><Detail label="Valid To" value={priceBook.validTo ? formatDate(text(priceBook.validTo)) : "-"} /><Detail label="Created" value={formatDateTime(text(priceBook.createdAt))} /><Detail label="Updated" value={formatDateTime(text(priceBook.updatedAt))} /></dl></Card><Card title="Description"><div className="whitespace-pre-wrap text-sm">{text(priceBook.description) || "No description."}</div></Card></div><Card title={`Products (${entries.length})`} action={<button className={primary} onClick={() => setEntryModal(true)}><Plus size={13} /> Add Product</button>}>{entries.length ? <div className="overflow-auto"><table className="min-w-full text-left text-sm"><thead className="bg-[#f3f3f3] text-xs"><tr><th className="px-3 py-2">Product</th><th className="px-3 py-2">List Price</th><th className="px-3 py-2">Currency</th><th className="px-3 py-2">State</th><th className="px-3 py-2">Actions</th></tr></thead><tbody>{entries.map((entry) => <tr key={id(entry)} className="border-t border-[#eef1f6]"><td className="px-3 py-2"><Link className="font-semibold text-brand-700 hover:underline" href={`/lightning/r/Product2/${text(entry.productId)}/view`}>{text((entry.product as RecordData | undefined)?.name)}</Link></td><td className="px-3 py-2">{money(entry.listPrice, text(entry.currency))}</td><td className="px-3 py-2">{text(entry.currency)}</td><td className="px-3 py-2"><Active value={entry.active} /></td><td className="px-3 py-2"><div className="flex gap-1"><button className={secondary} onClick={() => setEntryModal(entry)}>Edit</button><button className={secondary} onClick={() => void deleteEntry(entry)}><Trash2 size={12} /></button></div></td></tr>)}</tbody></table></div> : <Empty value="Add active Products and prices before using this Price Book in a Store or Invoice." />}</Card><Card title={`Connected Stores (${stores.length})`}>{stores.map((store) => <div key={id(store)} className="flex justify-between border-b border-[#eef1f6] py-2 text-sm last:border-0"><span>{text(store.name)}</span><span>{text(store.status)}</span></div>)}{!stores.length && <Empty value="No commerce store uses this Price Book." />}</Card>{edit && <PriceBookEditor priceBook={priceBook} onClose={() => setEdit(false)} onSaved={saveBook} />}{entryModal && <EntryEditor priceBook={priceBook} data={data} entry={entryModal === true ? undefined : entryModal} onClose={() => setEntryModal(null)} onSaved={saveEntry} />}</section>;
}

function Header({ icon, eyebrow, title, badge, actions }: { icon: ReactNode; eyebrow: string; title: string; badge: ReactNode; actions: ReactNode }) { return <div className="rounded-lg border border-[#e4e7ec] bg-white p-4 shadow-card"><div className="flex flex-wrap items-start justify-between gap-3"><div className="flex gap-3">{icon}<div><div className="text-xs text-[#706e6b]">{eyebrow}</div><h1 className="text-2xl font-semibold">{title}</h1><div className="mt-2">{badge}</div></div></div><div className="flex gap-2">{actions}</div></div></div>; }
function Empty({ value }: { value: string }) { return <div className="rounded border border-dashed border-[#d8dde6] p-5 text-center text-sm text-[#706e6b]">{value}</div>; }

function ProductEditor({ product, onClose, onSaved }: { product: RecordData; onClose: () => void; onSaved: (record: RecordData) => void }) { const [values, setValues] = useState({ name: text(product.name), family: text(product.family), productCode: text(product.productCode), sku: text(product.sku), category: text(product.category), active: Boolean(product.active), description: text(product.description) }); const [error, setError] = useState(""); async function save() { try { const payload = await json(`/api/records/Product2/${id(product)}`, { method: "PATCH", headers: { "Content-Type": "application/json" }, body: JSON.stringify(values) }); onSaved(payload.record as RecordData); } catch (saveError) { setError(saveError instanceof Error ? saveError.message : "Unable to save Product."); } } return <Modal title={`Edit ${text(product.name)}`} onClose={onClose} footer={<><button className={secondary} onClick={onClose}>Cancel</button><button className={primary} onClick={() => void save()}>Save</button></>}><div className="grid gap-3 md:grid-cols-2">{error && <div className="md:col-span-2 rounded border border-[#ea001e] bg-[#fff1f1] p-2 text-sm text-[#8e030f]">{error}</div>}<Field label="Name"><input className={input} value={values.name} onChange={(event) => setValues({ ...values, name: event.target.value })} /></Field><Field label="Family"><input className={input} value={values.family} onChange={(event) => setValues({ ...values, family: event.target.value })} /></Field><Field label="Product Code"><input className={input} value={values.productCode} onChange={(event) => setValues({ ...values, productCode: event.target.value })} /></Field><Field label="SKU"><input className={input} value={values.sku} onChange={(event) => setValues({ ...values, sku: event.target.value })} /></Field><Field label="Category"><input className={input} value={values.category} onChange={(event) => setValues({ ...values, category: event.target.value })} /></Field><label className="mt-6 flex items-center gap-2 text-sm"><input type="checkbox" checked={values.active} onChange={(event) => setValues({ ...values, active: event.target.checked })} /> Active</label><div className="md:col-span-2"><Field label="Description"><textarea className={cn(input, "min-h-24")} value={values.description} onChange={(event) => setValues({ ...values, description: event.target.value })} /></Field></div></div></Modal>; }
function PriceBookEditor({ priceBook, onClose, onSaved }: { priceBook: RecordData; onClose: () => void; onSaved: (record: RecordData) => void }) { const [values, setValues] = useState({ name: text(priceBook.name), active: Boolean(priceBook.active), description: text(priceBook.description), validFrom: text(priceBook.validFrom).slice(0, 10), validTo: text(priceBook.validTo).slice(0, 10) }); const [error, setError] = useState(""); async function save() { try { const payload = await json(`/api/records/Pricebook2/${id(priceBook)}`, { method: "PATCH", headers: { "Content-Type": "application/json" }, body: JSON.stringify(values) }); onSaved(payload.record as RecordData); } catch (saveError) { setError(saveError instanceof Error ? saveError.message : "Unable to save Price Book."); } } return <Modal title={`Edit ${text(priceBook.name)}`} onClose={onClose} footer={<><button className={secondary} onClick={onClose}>Cancel</button><button className={primary} onClick={() => void save()}>Save</button></>}><div className="space-y-3">{error && <div className="rounded border border-[#ea001e] bg-[#fff1f1] p-2 text-sm text-[#8e030f]">{error}</div>}<Field label="Name"><input className={input} value={values.name} onChange={(event) => setValues({ ...values, name: event.target.value })} /></Field><label className="flex items-center gap-2 text-sm"><input type="checkbox" checked={values.active} onChange={(event) => setValues({ ...values, active: event.target.checked })} /> Active</label><div className="grid gap-3 md:grid-cols-2"><Field label="Valid From"><input className={input} type="date" value={values.validFrom} onChange={(event) => setValues({ ...values, validFrom: event.target.value })} /></Field><Field label="Valid To"><input className={input} type="date" value={values.validTo} onChange={(event) => setValues({ ...values, validTo: event.target.value })} /></Field></div><Field label="Description"><textarea className={cn(input, "min-h-24")} value={values.description} onChange={(event) => setValues({ ...values, description: event.target.value })} /></Field></div></Modal>; }
function EntryEditor({ priceBook, data, entry, onClose, onSaved }: { priceBook: RecordData; data: BootstrapData; entry?: RecordData; onClose: () => void; onSaved: (record: RecordData) => void }) { const [values, setValues] = useState({ productId: text(entry?.productId), listPrice: text(entry?.listPrice), currency: text(entry?.currency) || "USD", active: entry?.active !== false }); const [error, setError] = useState(""); async function save() { try { const payload = await json(entry ? `/api/price-books/${id(priceBook)}/entries/${id(entry)}` : `/api/price-books/${id(priceBook)}/entries`, { method: entry ? "PATCH" : "POST", headers: { "Content-Type": "application/json" }, body: JSON.stringify(values) }); onSaved(payload.entry as RecordData); } catch (saveError) { setError(saveError instanceof Error ? saveError.message : "Unable to save entry."); } } return <Modal title={entry ? "Edit Price Book Entry" : "Add Product to Price Book"} onClose={onClose} footer={<><button className={secondary} onClick={onClose}>Cancel</button><button className={primary} onClick={() => void save()}>Save</button></>}><div className="space-y-3">{error && <div className="rounded border border-[#ea001e] bg-[#fff1f1] p-2 text-sm text-[#8e030f]">{error}</div>}<Field label="Product"><select className={input} disabled={Boolean(entry)} value={values.productId} onChange={(event) => setValues({ ...values, productId: event.target.value })}><option value="">Choose Product</option>{data.products.map((product) => <option key={id(product)} value={id(product)}>{text(product.name)}</option>)}</select></Field><div className="grid gap-3 md:grid-cols-2"><Field label="List Price"><input className={input} type="number" min="0" step="0.01" value={values.listPrice} onChange={(event) => setValues({ ...values, listPrice: event.target.value })} /></Field><Field label="Currency"><select className={input} disabled={Boolean(entry)} value={values.currency} onChange={(event) => setValues({ ...values, currency: event.target.value })}>{["USD", "AED", "EUR", "GBP"].map((value) => <option key={value}>{value}</option>)}</select></Field></div><label className="flex items-center gap-2 text-sm"><input type="checkbox" checked={values.active} onChange={(event) => setValues({ ...values, active: event.target.checked })} /> Active</label></div></Modal>; }
